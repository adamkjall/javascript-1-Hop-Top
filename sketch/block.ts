class Block extends GameObject {
  constructor(
    x: number,
    y: number,
    width: number,
    height: number,
    image: p5.Image = imgSolid
  ) {
    super(x, y, width, height, image);
  }

  
}
